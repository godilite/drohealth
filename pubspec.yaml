name: drohealth
description: DRO Health’s pharmacy.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


 
  cupertino_icons: ^1.0.0
  stacked: ^1.9.5
  flutter_svg: ^0.18.0
  get_it: ^5.0.6
  sliding_up_panel: ^1.0.2
  intl: ^0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/